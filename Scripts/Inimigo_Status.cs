using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Inimigo_Status : MonoBehaviour
{
    public float Vida;
    // Start is called before the first frame update
    void Start()
    {
        Vida = 100000;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
